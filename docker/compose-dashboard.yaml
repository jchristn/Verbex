services:
  verbex-dashboard:
    image: jchristn/verbex-dashboard:v0.1.0
    container_name: verbex-dashboard
    ports:
      - "8200:8200"
    volumes:
      - ./dashboard/logs:/app/logs
    restart: unless-stopped
