services:
  verbex-dashboard:
    image: jchristn77/verbex-dashboard:v0.2.1
    container_name: verbex-dashboard
    ports:
      - "8200:8200"
    volumes:
      - ./dashboard/logs:/app/logs
    environment:
      - VERBEX_SERVER_URL=http://verbex-server:8080
    restart: unless-stopped
